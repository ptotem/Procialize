<%= content_for :page_title do %>
    Speakers
<% end %>




<div data-role="collapsible-set">
  <% @speak.each do |s| %>
      <div data-role="collapsible" data-mini="true">
        <h6><%= s.name %></h6>
        <div class="fbinfo">
          <%# @a = @track_name.count %>
          <table>
            <tr>
              <td style="vertical-align: top;">
                <% if s.avatar.blank? %>
                    <%= image_tag "avatar.png", :style => 'max-height:100px;max-width:100px;margin:10px;'  %>
                <% else %>
                    <%= image_tag s.avatar.url, :style => 'max-height:100px;max-width:100px;margin:10px;' %>
                <% end %>
              </td>
            </tr>
            <tr>
              <td>
                <div style="font-size: 0.8em;font-weight: normal">
                <%#@count = @track_name.count %>
                <%# @countless=@count-1 %>
                <%# @track_name.each_with_index do |t, index|  %>
                    <%# unless t.name.blank? %>
                        <%# if index==0 %>
                            <!--Speaking at:-->
                        <%# end %>
                     <%#= t.name %><%# if index !=@countless %><%# end %>
                    <%# end %>
                <%# end %>
                  <a style="font-size: 0.8em;background: red;" href="<%= user_profile_path(s) %>" data-ajax=false data-theme='a'>
                  </a>
                <hr>
                <%= raw s.speaker_details %>
                </div>
              </td>
            </tr>
          </table>
        </div>
      </div>
  <% end %>
</div>










